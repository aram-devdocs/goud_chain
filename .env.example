# Goud Chain Environment Variables

# ======================
# MASTER CHAIN KEY (CRITICAL)
# ======================
# The master key used to encrypt block data.
#
# PRODUCTION: Set this to a 64-character hex string (32 bytes)
# Generate with: openssl rand -hex 32
# MASTER_CHAIN_KEY=a1b2c3d4e5f6...
#
# DEVELOPMENT: Falls back to passphrase-derived key if not set
MASTER_KEY_PASSPHRASE=goud_chain_dev_passphrase_v3_change_in_production

# ======================
# NODE CONFIGURATION
# ======================
# NODE_ID=node1
# HTTP_PORT=8080
# P2P_PORT=9000
# PEERS=node2:9000,node3:9000

# ======================
# JWT/SESSION
# ======================
# JWT_SECRET=your_jwt_secret_here
# SESSION_SECRET=your_session_secret_here  # Used to encrypt API keys in JWT tokens (auto-generated if not set)

# ======================
# SECURITY NOTES
# ======================
# 1. NEVER commit real keys to git
# 2. Use environment-specific .env files (.env.local, .env.production)
# 3. In production, use secrets management (Kubernetes secrets, AWS Secrets Manager, etc.)
# 4. Rotate keys periodically
# 5. Ensure all nodes use the SAME master key for chain synchronization
