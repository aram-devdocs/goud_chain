export enum ToastType {
  Success = 'success',
  Error = 'error',
  Warning = 'warning',
  Info = 'info',
}

export enum EventType {
  AccountCreate = 'account_create',
  AccountLogin = 'account_login',
  DataSubmit = 'data_submit',
  DataDecrypt = 'data_decrypt',
  DataList = 'data_list',
}

export enum ButtonVariant {
  Primary = 'primary',
  Secondary = 'secondary',
  Danger = 'danger',
  Ghost = 'ghost',
}

export enum ButtonSize {
  Small = 'sm',
  Medium = 'md',
  Large = 'lg',
}

export enum SpinnerSize {
  Small = 'sm',
  Medium = 'md',
  Large = 'lg',
}
